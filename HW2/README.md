If you have `swipl` installed on your system, just run:

```
swipl
[main].
[tests].

[bfs].

run_tests.
```

Which runs `swipl`, then loads `main.pl`, `bfs.pl`, and `tests.pl`. Then it runs the test defined in tests.pl.
